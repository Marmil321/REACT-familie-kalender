export type Pipe = {
  id: number;
  x: number;
  gapTop: number;
  width: number;
  gapHeight: number;
  height: number; // If still needed elsewhere
  gap: number;    // If still needed elsewhere
  left: number;   // If still needed elsewhere
};
